# kill-time

This repo contains a React + Vite frontend in `web/`.

## Common Commands

From the repo root:

```bash
npm run dev
npm run lint
npm run build
npm run preview
```

Or run the same commands directly from `web/`.
